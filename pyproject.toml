[project]
name = "autosat-tuning"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "jinja2>=3.1",
    "pyyaml>=6.0",
    "openai>=1.0",
    "requests>=2.31",
    "matplotlib>=3.7",
]

[dependency-groups]
dev = []
