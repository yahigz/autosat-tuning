{
  "run_id": "run_20260707_215834_369604_0c1b23b7",
  "metric_mode": "colors_par2",
  "metric_labels": {
    "primary": "colors",
    "secondary": "par2"
  },
  "baseline": {
    "compile_ok": true,
    "score": [
      451,
      4639.161837463002
    ],
    "all_ok": true,
    "total_colors": 451,
    "total_time": 4639.161837463002,
    "timed_out": 0,
    "par2": 4639.161837463002
  },
  "best": {
    "score": [
      473,
      3494.5482965679976
    ],
    "summary": {
      "compile_ok": true,
      "score": [
        473,
        3494.5482965679976
      ],
      "all_ok": true,
      "total_colors": 473,
      "total_time": 3494.5482965679976,
      "timed_out": 0,
      "par2": 3494.5482965679976
    },
    "updates": {
      "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
      "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
      "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
      "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int k = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
    },
    "rendered_source": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int k = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
    "trace_runs": [
      {
        "name": "DSJC500.1.col",
        "final_colors": 18,
        "final_time": 340.60151444998337,
        "trace": [],
        "final_timed_out": false,
        "final_par2": 340.60151444998337
      },
      {
        "name": "DSJC500.9.col",
        "final_colors": 139,
        "final_time": 961.567,
        "trace": [
          {
            "colors": 168,
            "time": 35.2208,
            "elapsed": 35.2208,
            "raw": "{\"colors\": 168, \"time\": 35.2208}",
            "captured_at": 1783452948.492108
          },
          {
            "colors": 167,
            "time": 35.2387,
            "elapsed": 35.2387,
            "raw": "{\"colors\": 167, \"time\": 35.2387}",
            "captured_at": 1783452948.510008
          },
          {
            "colors": 166,
            "time": 66.7412,
            "elapsed": 66.7412,
            "raw": "{\"colors\": 166, \"time\": 66.7412}",
            "captured_at": 1783452980.0125082
          },
          {
            "colors": 165,
            "time": 66.7653,
            "elapsed": 66.7653,
            "raw": "{\"colors\": 165, \"time\": 66.7653}",
            "captured_at": 1783452980.0366082
          },
          {
            "colors": 164,
            "time": 99.6284,
            "elapsed": 99.6284,
            "raw": "{\"colors\": 164, \"time\": 99.6284}",
            "captured_at": 1783453012.8997083
          },
          {
            "colors": 163,
            "time": 99.9676,
            "elapsed": 99.9676,
            "raw": "{\"colors\": 163, \"time\": 99.9676}",
            "captured_at": 1783453013.2389083
          },
          {
            "colors": 162,
            "time": 140.275,
            "elapsed": 140.275,
            "raw": "{\"colors\": 162, \"time\": 140.275}",
            "captured_at": 1783453053.5463083
          },
          {
            "colors": 161,
            "time": 188.591,
            "elapsed": 188.591,
            "raw": "{\"colors\": 161, \"time\": 188.591}",
            "captured_at": 1783453101.8623083
          },
          {
            "colors": 160,
            "time": 249.474,
            "elapsed": 249.474,
            "raw": "{\"colors\": 160, \"time\": 249.474}",
            "captured_at": 1783453162.7453082
          },
          {
            "colors": 159,
            "time": 298.46,
            "elapsed": 298.46,
            "raw": "{\"colors\": 159, \"time\": 298.46}",
            "captured_at": 1783453211.7313082
          },
          {
            "colors": 158,
            "time": 336.908,
            "elapsed": 336.908,
            "raw": "{\"colors\": 158, \"time\": 336.908}",
            "captured_at": 1783453250.1793082
          },
          {
            "colors": 157,
            "time": 336.943,
          },
          {
            "colors": 139,
            "time": 961.567,
            "elapsed": 961.567,
            "raw": "{\"colors\": 139, \"time\": 961.567}",
            "captured_at": 1783453874.838308
          }
        ],
        "final_timed_out": false,
        "final_par2": 961.567
      },
      {
        "name": "DSJR500.5.col",
        "final_colors": 62,
        "final_time": 572.9237821180141,
        "trace": [],
        "final_timed_out": false,
        "final_par2": 572.9237821180141
      },
      {
        "name": "gc_500_1",
        "final_colors": 17,
        "final_time": 162.107,
        "trace": [
          {
            "colors": 17,
            "time": 162.107,
            "elapsed": 162.107,
            "raw": "{\"colors\": 17, \"time\": 162.107}",
            "captured_at": 1783453075.3821328
          }
        ],
        "final_timed_out": false,
        "final_par2": 162.107
      },
      {
        "name": "gc_500_3",
        "final_colors": 35,
        "final_time": 271.918,
        "trace": [
          {
            "colors": 43,
            "time": 12.006,
            "elapsed": 12.006,
            "raw": "{\"colors\": 43, \"time\": 12.006}",
            "captured_at": 1783452925.2832608
          },
          {
            "colors": 42,
            "time": 83.568,
            "elapsed": 83.568,
            "raw": "{\"colors\": 42, \"time\": 83.568}",
            "captured_at": 1783452996.8452609
          },
          {
            "colors": 41,
            "time": 100.64,
            "elapsed": 100.64,
            "raw": "{\"colors\": 41, \"time\": 100.64}",
            "captured_at": 1783453013.917261
          },
          {
            "colors": 40,
            "time": 136.026,
            "elapsed": 136.026,
            "raw": "{\"colors\": 40, \"time\": 136.026}",
            "captured_at": 1783453049.3032608
          },
          {
            "colors": 39,
            "time": 173.617,
            "elapsed": 173.617,
            "raw": "{\"colors\": 39, \"time\": 173.617}",
            "captured_at": 1783453086.894261
          },
          {
            "colors": 38,
            "time": 190.352,
            "elapsed": 190.352,
            "raw": "{\"colors\": 38, \"time\": 190.352}",
            "captured_at": 1783453103.6292608
          },
          {
            "colors": 37,
            "time": 206.866,
            "elapsed": 206.866,
            "raw": "{\"colors\": 37, \"time\": 206.866}",
            "captured_at": 1783453120.1432607
          },
          {
            "colors": 36,
            "time": 226.588,
            "elapsed": 226.588,
            "raw": "{\"colors\": 36, \"time\": 226.588}",
            "captured_at": 1783453139.8652608
          },
          {
            "colors": 35,
            "time": 271.918,
            "elapsed": 271.918,
            "raw": "{\"colors\": 35, \"time\": 271.918}",
            "captured_at": 1783453185.1952608
          }
        ],
        "final_timed_out": false,
        "final_par2": 271.918
      },
      {
        "name": "gc_500_5",
        "final_colors": 60,
        "final_time": 203.118,
        "trace": [
          {
            "colors": 70,
            "time": 16.5188,
            "elapsed": 16.5188,
            "raw": "{\"colors\": 70, \"time\": 16.5188}",
            "captured_at": 1783452929.7972112
          },
          {
            "colors": 69,
            "time": 35.7844,
            "elapsed": 35.7844,
            "raw": "{\"colors\": 69, \"time\": 35.7844}",
            "captured_at": 1783452949.0628111
          },
          {
            "colors": 68,
            "time": 56.204,
            "elapsed": 56.204,
            "raw": "{\"colors\": 68, \"time\": 56.204}",
            "captured_at": 1783453664.5221567
          },
          {
            "colors": 148,
            "time": 751.333,
            "elapsed": 751.333,
            "raw": "{\"colors\": 148, \"time\": 751.333}",
            "captured_at": 1783453664.6111567
          },
          {
            "colors": 147,
            "time": 793.569,
            "elapsed": 793.569,
            "raw": "{\"colors\": 147, \"time\": 793.569}",
            "captured_at": 1783453706.8471568
          },
          {
            "colors": 146,
            "time": 828.459,
            "elapsed": 828.459,
            "raw": "{\"colors\": 146, \"time\": 828.459}",
            "captured_at": 1783453741.7371569
          },
          {
            "colors": 145,
            "time": 828.619,
            "elapsed": 828.619,
            "raw": "{\"colors\": 145, \"time\": 828.619}",
            "captured_at": 1783453741.8971567
          },
          {
            "colors": 144,
            "time": 864.691,
            "elapsed": 864.691,
            "raw": "{\"colors\": 144, \"time\": 864.691}",
            "captured_at": 1783453777.9691567
          },
          {
            "colors": 143,
            "time": 941.263,
            "elapsed": 941.263,
            "raw": "{\"colors\": 143, \"time\": 941.263}",
            "captured_at": 1783453854.5411568
          },
          {
            "colors": 142,
            "time": 982.313,
            "elapsed": 982.313,
            "raw": "{\"colors\": 142, \"time\": 982.313}",
            "captured_at": 1783453895.5911567
          }
        ],
        "final_timed_out": false,
        "final_par2": 982.313
      }
    ]
  },
  "iterations": [
    {
      "iteration": 0,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 1,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 2,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 3,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 4,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 5,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 6,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 7,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 8,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 9,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 10,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 11,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 12,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": false,
        "score": [
          1000000000,
          Infinity
        ]
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int max_color = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            // need to extend colors\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": null,
        "par2": null,
        "timed_out": null,
        "time": null
      }
    },
    {
      "iteration": 13,
      "task_mode": "__all__",
      "best_summary": {
        "compile_ok": true,
        "score": [
          473,
          3494.5482965679976
        ],
        "all_ok": true,
        "total_colors": 473,
        "total_time": 3494.5482965679976,
        "timed_out": 0,
        "par2": 3494.5482965679976
      },
      "best_updates": {
        "uvertex_function": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
        "cvertex_function": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
        "colour_function": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
        "greedy_initial_solution": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int k = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;"
      },
      "best_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int k = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "comparison_code": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {\n\nstruct Solver {\n    int n;\n    int m;\n    int k;\n    vector<vector<int>> graph;\n    vector<int> degree;\n    vector<vector<unsigned short>> forbidden;\n    vector<int> domain_size;\n    vector<int> colouring;\n    vector<int> remembered;\n    vector<char> in_C;\n    vector<int> bad_count;\n    mt19937_64 rng;\n    bool colour_prefers_different = true;\n\n    Solver(int n_, int m_, const vector<vector<int>>& graph_, int k_, uint64_t seed)\n    : n(n_),\n      m(m_),\n      k(k_),\n      graph(graph_),\n      degree(n_),\n      forbidden(n_, vector<unsigned short>(k_, 0)),\n      domain_size(n_, k_),\n      colouring(n_, -1),\n      remembered(n_, -1),\n      in_C(n_, 0),\n      bad_count(n_ * k_, 0),\n      rng(seed)\n      {\n        for (int v = 0; v < n; ++v) {\n            degree[v] = static_cast<int>(graph[v].size());\n        }\n        for (int v = 0; v < n; ++v) {\n            int single = singleton_color(v);\n            if (single != -1) {\n                add_blocking(v, -1, single);\n            }\n        }\n    }\n\n    int singleton_color(int v) const {\n        if (domain_size[v] != 1) {\n            return -1;\n        }\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[v][c] == 0) {\n                return c;\n            }\n        }\n        return -1;\n    }\n\n    int bad(int v, int c) const {\n        return bad_count[v * k + c];\n    }\n\n    void add_blocking(int v, int old_single, int new_single) {\n        if (old_single == new_single) {\n            return;\n        }\n        if (old_single != -1) {\n            for (int to : graph[v]) {\n                --bad_count[to * k + old_single];\n            }\n        }\n        if (new_single != -1) {\n            for (int to : graph[v]) {\n                ++bad_count[to * k + new_single];\n            }\n        }\n    }\n\n    int U_size() const {\n        int cnt = 0;\n        for (char is_in_C : in_C) {\n            if (!is_in_C) {\n                ++cnt;\n            }\n        }\n        return cnt;\n    }\n\n    int C_size() const {\n        return n - U_size();\n    }\n\n    void uncolour_vertex_and_update_domains(int v) {\n        int c = colouring[v];\n        in_C[v] = 0;\n        colouring[v] = -1;\n\n        int single = singleton_color(v);\n        if (single != -1) {\n            add_blocking(v, -1, single);\n        }\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] > 0) {\n                if (forbidden[to][c] == 1) {\n                    ++domain_size[to];\n                }\n                --forbidden[to][c];\n            }\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    void colour_vertex_and_update_domains(int v, int c) {\n        if (!in_C[v]) {\n            int old_single = singleton_color(v);\n            if (old_single != -1) {\n                add_blocking(v, old_single, -1);\n            }\n        }\n\n        in_C[v] = 1;\n        colouring[v] = c;\n        remembered[v] = c;\n\n        for (int to : graph[v]) {\n            int old_domain = domain_size[to];\n            int old_single = -1;\n            if (!in_C[to] && old_domain == 1) {\n                old_single = singleton_color(to);\n            }\n\n            if (forbidden[to][c] == 0) {\n                --domain_size[to];\n            }\n            ++forbidden[to][c];\n\n            int new_domain = domain_size[to];\n            int new_single = -1;\n            if (!in_C[to] && new_domain == 1) {\n                new_single = singleton_color(to);\n            }\n            if (!in_C[to]) {\n                add_blocking(to, old_single, new_single);\n            }\n        }\n    }\n\n    int uncolored_degree(int v) const {\n        int result = 0;\n        for (int to : graph[v]) {\n            if (!in_C[to]) {\n                ++result;\n            }\n        }\n        return result;\n    }\n\n    int UVERTEX() {\nvector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];\n    }\n\n    int CVERTEX() {\nint best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];\n    }\n\n    vector<int> build_D(int u) const {\n        vector<int> result;\n        for (int c = 0; c < k; ++c) {\n            if (forbidden[u][c] == 0 && bad(u, c) == 0) {\n                result.push_back(c);\n            }\n        }\n        return result;\n    }\n\n    int COLOUR(int u, const vector<int>& D) {\nif (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();\n    }\n\n    pair<int, vector<int>> FCNS(int B, int max_steps) {\n        int steps = 0;\n        while (steps < max_steps) {\n            if (U_size() == 0) {\n                break;\n            }\n\n            int u = UVERTEX();\n            if (u < 0) {\n                break;\n            }\n\n            vector<int> D = build_D(u);\n            if (!D.empty()) {\n                int c = COLOUR(u, D);\n                colour_vertex_and_update_domains(u, c);\n            } else {\n                int limit = min(B, C_size());\n                for (int i = 0; i < limit; ++i) {\n                    int c_vertex = CVERTEX();\n                    if (c_vertex < 0) {\n                        break;\n                    }\n                    uncolour_vertex_and_update_domains(c_vertex);\n                    colour_prefers_different = true;\n                }\n            }\n\n            ++steps;\n        }\n\n        vector<int> result = colouring;\n        int used_colors = 0;\n        vector<int> remap(k, -1);\n        for (int v = 0; v < n; ++v) {\n            if (result[v] < 0) {\n                continue;\n            }\n            if (remap[result[v]] == -1) {\n                remap[result[v]] = used_colors++;\n            }\n            result[v] = remap[result[v]];\n        }\n        return {used_colors, result};\n    }\n};\n\nvector<int> greedy_coloring(const vector<vector<int>>& graph) {\nint n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int k = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;\n}\n\nint count_used_colors(const vector<int>& colors) {\n    int used = 0;\n    for (int c : colors) {\n        used = max(used, c + 1);\n    }\n    return used;\n}\n\nvector<int> make_best_coloring(const vector<vector<int>>& graph,\n                               const chrono::steady_clock::time_point& launch_time) {\n    int n = static_cast<int>(graph.size());\n    vector<int> best = greedy_coloring(graph);\n    int best_colors = count_used_colors(best);\n\n    uint64_t base_seed = static_cast<uint64_t>(chrono::high_resolution_clock::now().time_since_epoch().count());\n    vector<int> b_values = {1, 2, 4, 6, 8, 15, 25, 40};\n    sort(b_values.begin(), b_values.end());\n    b_values.erase(unique(b_values.begin(), b_values.end()), b_values.end());\n\n    int attempts = 200;\n    int target_k = best_colors - 1;\n    for (int attempt = 0; attempt < attempts && target_k >= 1; ++attempt) {\n        int b = b_values[attempt % static_cast<int>(b_values.size())];\n        int max_steps = 1000 * n;\n        uint64_t seed = base_seed + static_cast<uint64_t>(attempt) * 0x9e3779b97f4a7c15ULL;\n        Solver solver(n, 0, graph, target_k, seed);\n        auto [used_colors, colors] = solver.FCNS(b, max_steps);\n        if (static_cast<int>(colors.size()) != n) {\n            // it means that the solver failed to color all vertices, so we skip this attempt\n            continue;\n        }\n\n        bool complete = true;\n        for (int v = 0; v < n; ++v) {\n            if (colors[v] < 0) {\n                complete = false;\n                // it means that the solver failed to color all vertices, so we skip this attempt\n                break;\n            }\n        }\n        if (!complete) {\n            continue;\n        }\n        --target_k;\n        if (used_colors < best_colors) {\n            best = std::move(colors);\n            best_colors = used_colors;\n            const double elapsed_seconds = chrono::duration<double>(\n                chrono::steady_clock::now() - launch_time\n            ).count();\n            cerr << \"{\\\"colors\\\": \" << best_colors\n                 << \", \\\"time\\\": \" << elapsed_seconds << \"}\" << endl;\n        }\n    }\n\n    return best;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const auto launch_time = chrono::steady_clock::now();\n\n    int n, m;\n    cin >> n >> m;\n    vector<vector<int>> graph(n);\n    for (int i = 0; i < m; ++i) {\n        int u, v;\n        cin >> u >> v;\n        graph[u].push_back(v);\n        graph[v].push_back(u);\n    }\n\n    vector<int> answer = make_best_coloring(graph, launch_time);\n    int colors_used = count_used_colors(answer);\n\n    cout << colors_used << '\\n';\n    for (int v = 0; v < n; ++v) {\n        cout << answer[v] + 1 << ' ';\n    }\n    cout << '\\n';\n    return 0;\n}\n",
      "code_preview": "#include <algorithm>\n#include <chrono>\n#include <iostream>\n#include <limits>\n#include <numeric>\n#include <random>\n#include <set>\n#include <utility>\n#include <vector>\n\nusing namespace std;\n\nnamespace {",
      "best_prompt_context": {
        "implementations": [
          {
            "task": "uvertex_function",
            "code": "vector<int> candidates;\n        vector<int> fallback;\n        for (int v = 0; v < n; ++v) {\n            if (in_C[v]) {\n                continue;\n            }\n            fallback.push_back(v);\n            if (domain_size[v] > 1) {\n                candidates.push_back(v);\n            }\n        }\n        const vector<int>& pool = candidates.empty() ? fallback : candidates;\n        uniform_int_distribution<int> dist(0, static_cast<int>(pool.size()) - 1);\n        return pool[dist(rng)];",
            "title": "Baseline UVERTEX",
            "reason": "No improvement found for UVERTEX."
          },
          {
            "task": "cvertex_function",
            "code": "int best_domain = -1;\n        int best_degree = numeric_limits<int>::max();\n        vector<int> candidates;\n        for (int v = 0; v < n; ++v) {\n            if (!in_C[v]) {\n                continue;\n            }\n            if (domain_size[v] > best_domain) {\n                best_domain = domain_size[v];\n                best_degree = degree[v];\n                candidates.clear();\n                candidates.push_back(v);\n            } else if (domain_size[v] == best_domain) {\n                if (degree[v] < best_degree) {\n                    best_degree = degree[v];\n                    candidates.clear();\n                    candidates.push_back(v);\n                } else if (degree[v] == best_degree) {\n                    candidates.push_back(v);\n                }\n            }\n        }\n        uniform_int_distribution<int> dist(0, static_cast<int>(candidates.size()) - 1);\n        return candidates[dist(rng)];",
            "title": "Baseline CVERTEX",
            "reason": "No improvement found for CVERTEX."
          },
          {
            "task": "colour_function",
            "code": "if (D.empty()) {\n            return -1;\n        }\n        vector<int> shuffled = D;\n        shuffle(shuffled.begin(), shuffled.end(), rng);\n        int remembered_color = remembered[u];\n        if (colour_prefers_different) {\n            for (int c : shuffled) {\n                if (c != remembered_color) {\n                    colour_prefers_different = false;\n                    return c;\n                }\n            }\n            return shuffled.front();\n        }\n        for (int c : shuffled) {\n            if (c == remembered_color) {\n                return c;\n            }\n        }\n        return shuffled.front();",
            "title": "Baseline COLOUR",
            "reason": "No improvement found for COLOUR."
          },
          {
            "task": "greedy_initial_solution",
            "code": "int n = static_cast<int>(graph.size());\n    vector<int> color(n, -1);\n    vector<int> saturation(n, 0);\n    vector<int> degree(n);\n    for (int i = 0; i < n; ++i) {\n        degree[i] = static_cast<int>(graph[i].size());\n    }\n    vector<bool> used_color(n, false);\n    int k = 0;\n    for (int i = 0; i < n; ++i) {\n        int best = -1;\n        int best_sat = -1;\n        int best_deg = -1;\n        for (int v = 0; v < n; ++v) {\n            if (color[v] != -1) continue;\n            int sat = saturation[v];\n            if (sat > best_sat || (sat == best_sat && degree[v] > best_deg)) {\n                best_sat = sat;\n                best_deg = degree[v];\n                best = v;\n            }\n        }\n        if (best == -1) break;\n        vector<bool> used(k, false);\n        for (int to : graph[best]) {\n            if (color[to] != -1) {\n                used[color[to]] = true;\n            }\n        }\n        int chosen = 0;\n        while (chosen < k && used[chosen]) ++chosen;\n        color[best] = chosen;\n        if (chosen >= k) {\n            color[best] = k;\n            k++;\n        }\n        for (int to : graph[best]) {\n            if (color[to] == -1) {\n                saturation[to]++;\n            }\n        }\n    }\n    return color;",
            "title": "DSATUR greedy initial solution",
            "reason": "DSATUR is a classic greedy heuristic that often produces better initial colorings than RLF, potentially reducing the number of colors needed."
          }
        ],
        "colors": 473,
        "par2": 3494.5482965679976,
        "timed_out": 0,
        "time": 3494.5482965679976
      }
    }
  ],
  "iteration": 13
}