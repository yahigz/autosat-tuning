{
  "0": {
    "time": 8041.352000000001,
    "PAR-2": 8041.352000000001,
    "prompt": ""
  },
  "1": {
    "time": 23206.370000000003,
    "PAR-2": 41170.664000000004,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Additional exponential decay for older variables to prevent stagnation\\nif (conflicts % 1000 == 0) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 0.95;\\n    var_inc *= 0.95;\\n}\", \"title\": \"Periodic activity decay\", \"reason\": \"Periodically decaying all activities prevents older variables from dominating indefinitely, encouraging exploration of newer conflict-related variables.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 50) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 10) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0) for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\", \"title\": \"Adjusted rephase probabilities\", \"reason\": \"Increasing the chance to use local_best and its negation improves phase reuse, while reducing random phases to avoid excessive noise.\"}, \"rephase_function\": {\"code\": \"rephases = 0, threshold *= 0.85, rephase_limit += 4096;\", \"title\": \"Faster threshold decay and smaller rephase limit increment\", \"reason\": \"More aggressive threshold decay and smaller rephase limit increment allow more frequent rephasing, helping escape local optima faster.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighter restart condition\", \"reason\": \"Increasing the multiplier from 0.8 to 0.9 makes the condition stricter, reducing unnecessary restarts and allowing more focused search.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay rephase until after initial search\", \"reason\": \"Adding a conflict threshold prevents premature rephasing early in the search, allowing the solver to build a stable phase pattern first.\"}}"
  },
  "2": {
    "time": 18091.976000000002,
    "PAR-2": 33091.97,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Additional exponential decay to prevent stagnation\\nif (var_inc > 1e10) {\\n    var_inc *= 0.5;\\n}\", \"title\": \"Exponential decay cap for var_inc\", \"reason\": \"Prevents var_inc from growing too large, which can cause overly aggressive bumping and loss of search diversity.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Increase rephase_limit more aggressively to reduce rephase frequency\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "3": {
    "time": 18061.669,
    "PAR-2": 33061.664000000004,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Decay var_inc more aggressively to prevent stagnation\\nif (var_inc > 1e8) {\\n    var_inc *= 0.9;\\n}\", \"title\": \"Softer var_inc cap\", \"reason\": \"Previous cap at 1e10 with 0.5 decay was too aggressive; using 1e8 with 0.9 decay provides smoother control and maintains search diversity.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "4": {
    "time": 13182.485,
    "PAR-2": 13182.485,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive var_inc decay based on conflict frequency\\nif (conflicts % 1000 == 0 && var_inc > 1e6) {\\n    var_inc *= 0.95;\\n}\", \"title\": \"Adaptive var_inc decay on conflict milestones\", \"reason\": \"Decaying var_inc every 1000 conflicts prevents unbounded growth while maintaining VSIDS responsiveness, improving search balance.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "5": {
    "time": 9739.416,
    "PAR-2": 9739.416,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Decay var_inc more aggressively when conflict rate is high\\nif (conflicts % 500 == 0 && var_inc > 1e5) {\\n    var_inc *= 0.9;\\n}\", \"title\": \"Adaptive var_inc decay with conflict rate\", \"reason\": \"Decaying var_inc every 500 conflicts when it exceeds 1e5 prevents unbounded growth and maintains VSIDS responsiveness, improving search balance.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "6": {
    "time": 12853.044,
    "PAR-2": 12853.044,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive decay based on conflict frequency\\nif (conflicts % 1000 == 0 && var_inc > 1e4) {\\n    var_inc *= 0.95;\\n}\", \"title\": \"Adaptive var_inc decay with conflict rate\", \"reason\": \"Decaying var_inc every 1000 conflicts when it exceeds 1e4 prevents unbounded growth and maintains VSIDS responsiveness, improving search balance.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "7": {
    "time": 22070.088000000003,
    "PAR-2": 43070.075,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive decay based on conflict rate and activity magnitude\\nif (conflicts % 1000 == 0 && var_inc > 1e4) {\\n    var_inc *= 0.95;\\n}\\n// Additional decay when activity is very high to prevent stagnation\\nif (activity[var] > 1e50) {\\n    var_inc *= 0.9;\\n}\", \"title\": \"Adaptive var_inc decay with conflict rate and activity cap\", \"reason\": \"Decaying var_inc every 1000 conflicts when it exceeds 1e4 prevents unbounded growth and maintains VSIDS responsiveness. Additionally, decaying var_inc when a variable's activity becomes extremely high helps avoid over-prioritization of a single variable, improving search balance.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "8": {
    "time": 12840.761,
    "PAR-2": 12840.761,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Decay var_inc based on conflict rate to prevent over-prioritization\\nif (conflicts % 1000 == 0 && var_inc > 1e4) {\\n    var_inc *= 0.95;\\n}\", \"title\": \"Adaptive var_inc decay with conflict rate\", \"reason\": \"Decaying var_inc every 1000 conflicts when it exceeds 1e4 prevents unbounded growth and maintains VSIDS responsiveness, improving search balance.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "9": {
    "time": 11977.365,
    "PAR-2": 11977.365,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive decay: reduce var_inc when conflict rate is high to prevent over-prioritization\\nif (conflicts % 500 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.9;\\n}\", \"title\": \"Adaptive var_inc decay with conflict rate\", \"reason\": \"Decaying var_inc every 500 conflicts when it exceeds 1e3 prevents unbounded growth and maintains VSIDS responsiveness, improving search balance.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "10": {
    "time": 15238.509,
    "PAR-2": 15238.509,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive decay based on conflict rate: reduce var_inc when conflict rate is high\\nif (conflicts % 1000 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.95;\\n}\", \"title\": \"Adaptive var_inc decay with conflict rate\", \"reason\": \"Decaying var_inc every 1000 conflicts when it exceeds 1e3 prevents unbounded growth and maintains VSIDS responsiveness, improving search balance.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "11": {
    "time": 15334.307999999999,
    "PAR-2": 15334.307999999999,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive var_inc decay based on conflict rate and recent activity\\nif (conflicts % 1000 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.95;\\n}\\n// Boost var_inc when conflict rate is low to encourage exploration\\nif (conflicts % 500 == 0 && var_inc < 1e-3) {\\n    var_inc *= 1.05;\\n}\", \"title\": \"Adaptive var_inc with dual decay and boost\", \"reason\": \"Combines decay when var_inc grows too large with a boost when it becomes too small, maintaining a balanced VSIDS activity range for better search focus.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "12": {
    "time": 15254.054,
    "PAR-2": 15254.054,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive var_inc decay based on conflict rate and recent activity\\nif (conflicts % 1000 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.95;\\n}\\n// Boost var_inc when conflict rate is low to encourage exploration\\nif (conflicts % 500 == 0 && var_inc < 1e-3) {\\n    var_inc *= 1.05;\\n}\", \"title\": \"Adaptive var_inc with dual decay and boost\", \"reason\": \"Combines decay when var_inc grows too large with a boost when it becomes too small, maintaining a balanced VSIDS activity range for better search focus.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "13": {
    "time": 15112.455999999998,
    "PAR-2": 15112.455999999998,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive var_inc decay based on conflict rate and recent activity\\nif (conflicts % 1000 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.95;\\n}\\n// Boost var_inc when conflict rate is low to encourage exploration\\nif (conflicts % 500 == 0 && var_inc < 1e-3) {\\n    var_inc *= 1.05;\\n}\", \"title\": \"Adaptive var_inc with dual decay and boost\", \"reason\": \"Combines decay when var_inc grows too large with a boost when it becomes too small, maintaining a balanced VSIDS activity range for better search focus.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "14": {
    "time": 15246.133999999998,
    "PAR-2": 15246.133999999998,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive var_inc decay based on conflict rate and recent activity\\nif (conflicts % 1000 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.95;\\n}\\n// Boost var_inc when conflict rate is low to encourage exploration\\nif (conflicts % 500 == 0 && var_inc < 1e-3) {\\n    var_inc *= 1.05;\\n}\", \"title\": \"Adaptive var_inc with dual decay and boost\", \"reason\": \"Combines decay when var_inc grows too large with a boost when it becomes too small, maintaining a balanced VSIDS activity range for better search focus.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "15": {
    "time": 15266.002,
    "PAR-2": 15266.002,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive var_inc decay based on conflict rate and recent activity\\nif (conflicts % 1000 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.95;\\n}\\n// Boost var_inc when conflict rate is low to encourage exploration\\nif (conflicts % 500 == 0 && var_inc < 1e-3) {\\n    var_inc *= 1.05;\\n}\", \"title\": \"Adaptive var_inc with dual decay and boost\", \"reason\": \"Combines decay when var_inc grows too large with a boost when it becomes too small, maintaining a balanced VSIDS activity range for better search focus.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "16": {
    "time": 16823.732,
    "PAR-2": 16823.732,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive var_inc decay based on conflict rate and recent activity\\nif (conflicts % 1000 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.95;\\n}\\n// Boost var_inc when conflict rate is low to encourage exploration\\nif (conflicts % 500 == 0 && var_inc < 1e-3) {\\n    var_inc *= 1.05;\\n}\", \"title\": \"Adaptive var_inc with dual decay and boost\", \"reason\": \"Combines decay when var_inc grows too large with a boost when it becomes too small, maintaining a balanced VSIDS activity range for better search focus.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "17": {
    "time": 17838.445,
    "PAR-2": 17838.445,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive var_inc decay based on conflict rate and recent activity\\nif (conflicts % 1000 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.95;\\n}\\n// Boost var_inc when conflict rate is low to encourage exploration\\nif (conflicts % 500 == 0 && var_inc < 1e-3) {\\n    var_inc *= 1.05;\\n}\", \"title\": \"Adaptive var_inc with dual decay and boost\", \"reason\": \"Combines decay when var_inc grows too large with a boost when it becomes too small, maintaining a balanced VSIDS activity range for better search focus.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "18": {
    "time": 13320.326999999997,
    "PAR-2": 13320.326999999997,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive var_inc decay based on conflict rate and recent activity\\nif (conflicts % 1000 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.95;\\n}\\n// Boost var_inc when conflict rate is low to encourage exploration\\nif (conflicts % 500 == 0 && var_inc < 1e-3) {\\n    var_inc *= 1.05;\\n}\", \"title\": \"Adaptive var_inc with dual decay and boost\", \"reason\": \"Combines decay when var_inc grows too large with a boost when it becomes too small, maintaining a balanced VSIDS activity range for better search focus.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "19": {
    "time": 14200.074,
    "PAR-2": 14200.074,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive var_inc decay based on conflict rate and recent activity\\nif (conflicts % 1000 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.95;\\n}\\n// Boost var_inc when conflict rate is low to encourage exploration\\nif (conflicts % 500 == 0 && var_inc < 1e-3) {\\n    var_inc *= 1.05;\\n}\", \"title\": \"Adaptive var_inc with dual decay and boost\", \"reason\": \"Combines decay when var_inc grows too large with a boost when it becomes too small, maintaining a balanced VSIDS activity range for better search focus.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  },
  "20": {
    "time": 14409.709999999997,
    "PAR-2": 14409.709999999997,
    "prompt": "{\"bump_var_function\": {\"code\": \"if ((activity[var] += var_inc * coeff) > 1e100) {\\n    for (int i = 1; i <= vars; i++) activity[i] *= 1e-100;\\n    var_inc *= 1e-100;\\n}\\nif (vsids.inHeap(var)) vsids.update(var);\\n// Adaptive var_inc decay based on conflict rate and recent activity\\nif (conflicts % 1000 == 0 && var_inc > 1e3) {\\n    var_inc *= 0.95;\\n}\\n// Boost var_inc when conflict rate is low to encourage exploration\\nif (conflicts % 500 == 0 && var_inc < 1e-3) {\\n    var_inc *= 1.05;\\n}\", \"title\": \"Adaptive var_inc with dual decay and boost\", \"reason\": \"Combines decay when var_inc grows too large with a boost when it becomes too small, maintaining a balanced VSIDS activity range for better search focus.\"}, \"restart_function\": {\"code\": \"fast_lbd_sum = lbd_queue_size = lbd_queue_pos = 0;\\nbacktrack(0);\\nint phase_rand = rand() % 100;\\nif ((phase_rand -= 60) < 0)     for (int i = 1; i <= vars; i++) saved[i] = local_best[i];\\nelse if ((phase_rand -= 5) < 0) for (int i = 1; i <= vars; i++) saved[i] = -local_best[i];\\nelse if ((phase_rand -= 20) < 0)for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\nelse ;\\n// Add a small random perturbation to break symmetry\\nif (rand() % 100 < 10) {\\n    for (int i = 1; i <= vars; i++) saved[i] = rand() % 2 ? 1 : -1;\\n}\", \"title\": \"Random perturbation on restart\", \"reason\": \"Adds occasional full randomization to phase saving to escape local minima and improve exploration.\"}, \"rephase_function\": {\"code\": \"rephases = 0;\\nthreshold *= 0.9;\\nrephase_limit += 8192;\\n// Cap rephase_limit to prevent excessive growth\\nif (rephase_limit > 100000) rephase_limit = 100000;\", \"title\": \"Cap rephase_limit\", \"reason\": \"Prevents rephase_limit from growing unbounded, ensuring rephases still occur at reasonable intervals.\"}, \"restart_condition\": {\"code\": \"else if (lbd_queue_size == 50 && 0.9 * fast_lbd_sum / lbd_queue_size > slow_lbd_sum / conflicts) restart();\", \"title\": \"Tighten restart condition\", \"reason\": \"Increases the threshold from 0.8 to 0.9 to make restarts slightly less frequent, allowing more time for search to converge.\"}, \"rephase_condition\": {\"code\": \"else if (rephases >= rephase_limit && conflicts > 1000) rephase();\", \"title\": \"Delay initial rephase\", \"reason\": \"Prevents rephasing too early when the solver has not yet gathered enough conflict information, improving early search stability.\"}}"
  }
}